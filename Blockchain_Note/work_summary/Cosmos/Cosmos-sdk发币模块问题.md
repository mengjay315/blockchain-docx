

链的权益代币总量是一定的，这个是在哪里配置的？

目前新增的 `token`模块，功能有：发行，增发，销毁，冻结

通过这个模块发行的代币是否要有总量的限制？

通过这个模块发行的代币，除了可以用来作为费用代币之外，还可以做什么？

链上主网，只有一种权益代币，可以上交易所，公开发行，

谁都可以使用zchain-sdk做二次开发

假设我使用zchain-sdk开发一个dapp,那么可以利用sdk中的token模块，发行与dapp相关的代币，

如果增加DEX(去中心化交易所)模块，使用DEX模块开发一个去中心化交易所，那么原来token模块发行的代币可以上去中心化交易所吗？













