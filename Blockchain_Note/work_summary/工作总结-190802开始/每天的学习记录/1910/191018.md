## 191018

Deploy that packaged Node JS projec  tarball 

-------

在浏览器上加一个模块， asset资产模块，

功能：查询账户下的资产信息，

这个功能不在浏览器中实现，应该在钱包中实现

----

接下来继续看cosmos-sdk

 ps -ef|grep .pl|kill -9 `awk '{print $2}'`

------

本周完成工作：

1. 熟悉了cosmos-sdk的代码，私钥管理部分，学习了sdk的其他模块。
2. 学习了基于cosmos-sdk的开源区块链浏览器，知道了怎么配置，在本地可以运行，部署在开发环境正在研究。

