## 190926

关于cosmos-sdk的nameservice的例子，转账的具体过程代码在哪里？我还没找到。

创世文件中账户配置的扣交易费的token，**名称不能包含大写字符**，例如：100atoken可以， 100Atoken就不行

----

```bash
In a real environment the raw transaction should be signed on the client side. Also the sequence needs to be adjusted, depending on what the query of alice's account has shown.
```

