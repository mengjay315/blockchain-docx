# 需要回顾的知识
1. go语言中的 for ... range 循环遍历
2. switch ... case 语句
3. go语言结构体的序列化与反序列化
4. 切片的初始化，map 的初始化

这个result结构体是怎么定义的， 后边还有一个 {}， 是什么意思？

```go

result := struct {
		Current struct {
			Key   string `json:"key"`
			Value string `json:"value"`
		} `json:"current"`
		Historys []History `json:"historys"`
	}{}
```

![](_v_images/20190611132923669_581281457.png)