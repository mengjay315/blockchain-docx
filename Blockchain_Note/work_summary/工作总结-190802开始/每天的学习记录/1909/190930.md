## 190930

cosmohub上的 gaiad, gaiacli

进行转账测试，如果app.toml中指定了最小的gaa-prices，则在转账时如果设置的最小gas介个小于配置文件中的，则交易失败

但是在基于cosmos-sdk搭建的域名服务的例子中，app.toml配置中设置了最小gas-prices,在命令行执行转账交易时，不管在命令行我指定的gas-prices是低于配置或者高于配置，交易都可以成功。

**接下来看send的代码，看看与gas-prices的部分，**

